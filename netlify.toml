[build]
  publish = "."

# Keep root on the physical index.html file to avoid rewrite issues.
# This is the safest static setup for Netlify preview and production.
[[redirects]]
  from = "/applicant-manage-applications"
  to = "/index.html"
  status = 200
